import React from "react";
import { Divider, Typography } from "@material-ui/core";

export const AppFooter = () => (
    <div>
        <Typography variant="body1">Fluid Outliner cares about your privacy. </Typography>
        <Divider/>
        <Typography variant="body1">Github | @lorefnon</Typography>
    </div>

)